# 🧱 BADWALL - Telegram бот для модерации чата

Telegram бот на aiogram для автоматической проверки сообщений на наличие нецензурной лексики с помощью библиотеки [check-swear](https://github.com/bbd03/check-swear).

## Возможности

### Модерация сообщений
- ✅ Автоматическая проверка всех сообщений в групповых чатах на мат
- ✅ Удаление сообщений с вероятностью мата ≥ 0.7 (только в группах)
- ✅ Использование ML-модели (SVM) для определения контекста
- ✅ Поддержка русского языка и транслитерации
- ✅ **Бан пользователей за мат** (автоматический)
- ✅ Проверка и удаление сообщений со ссылками (URL, t.me, telegram.me)
- ✅ Проверка на запрещенные символы (спецсимволы и нерусские буквы, **эмодзи разрешены**)
- ✅ **Расшифровка голосовых сообщений** и проверка их на мат

### Уведомления и управление
- ✅ **Уведомления админам о бане** с информацией о пользователе и причине
- ✅ **Инлайн-кнопки для разбана** пользователей прямо из уведомления
- ✅ Кнопка скрытия уведомлений о бане

### Статистика и безопасность
- ✅ Ежедневная статистика работы бота (отправляется админам в личные сообщения в 21:00)
- ✅ Детализированная статистика по типам удалений (мат, ссылки, запрещенные символы)
- ✅ Статистика по количеству банов
- ✅ Работа только в указанных групповых чатах (безопасность)
- ✅ Исключение администраторов и создателей чата из проверок
- ✅ Команды бота работают только в личных сообщениях

## Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта

5. Получите chat_id чата, где будет работать бот:
   - Добавьте бота в группу
   - Отправьте любое сообщение в группу
   - Перейдите по ссылке: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Найдите `"chat":{"id":-123456789}` - это и есть chat_id (может быть отрицательным для групп)
   - Или используйте бота [@userinfobot](https://t.me/userinfobot) - добавьте его в группу и он покажет chat_id

6. Настройте `.env` файл:
```
BOT_TOKEN=your_bot_token_here
PROFANITY_THRESHOLD=0.7
ALLOWED_CHAT_IDS=-123456789,-987654321
ADMIN_IDS=123456789,987654321
```
   - `BOT_TOKEN` - токен бота от BotFather
   - `PROFANITY_THRESHOLD` - порог вероятности мата (рекомендуется 0.7)
   - `ALLOWED_CHAT_IDS` - список chat_id разрешенных чатов через запятую (без пробелов)
   - `ADMIN_IDS` - список user_id админов для получения статистики через запятую (без пробелов)

## Настройка бота в группе

Для корректной работы бота необходимо:

1. Добавить бота в группу как администратора
2. Выдать боту следующие права:
   - **Удалять сообщения** (обязательно)
   - **Банить пользователей** (для функции бана за мат)
   - **Просматривать участников** (для проверки прав администраторов)

   Как выдать права:
   - Откройте настройки группы
   - Перейдите в "Администраторы"
   - Выберите бота
   - Включите необходимые права

## Дополнительные требования

### Для расшифровки голосовых сообщений

Бот поддерживает расшифровку голосовых сообщений через Google Speech Recognition. Для работы этой функции:

1. Установите зависимости (уже включены в `requirements.txt`):
   ```bash
   pip install SpeechRecognition pydub
   ```

2. Установите `ffmpeg` (требуется для конвертации аудио):
   - **Windows**: Скачайте с [ffmpeg.org](https://ffmpeg.org/download.html) и добавьте в PATH
   - **Linux**: `sudo apt-get install ffmpeg`
   - **macOS**: `brew install ffmpeg`

3. **Важно**: Для работы Google Speech Recognition требуется интернет-соединение

**Примечание**: Если `ffmpeg` не установлен, расшифровка голосовых сообщений будет недоступна, но остальные функции бота будут работать нормально.

## Запуск

### Обычный запуск

```bash
python bot.py
```

### Запуск в Docker

1. Убедитесь, что у вас установлены Docker и Docker Compose

2. Соберите образ:
```bash
docker-compose build
```

3. Запустите контейнер:
```bash
docker-compose up -d
```

4. Просмотр логов:
```bash
docker-compose logs -f
```

5. Остановка:
```bash
docker-compose down
```

**Примечание:** 
- Убедитесь, что файл `.env` находится в корне проекта и содержит все необходимые переменные
- Docker Compose автоматически загружает переменные из `.env` файла
- Контейнер автоматически перезапускается при сбое (`restart: unless-stopped`)

## Структура проекта

```
BADWALL/
├── bot.py                      # Основной файл запуска бота
├── config.py                   # Конфигурация (токен, пороги, админы)
├── requirements.txt            # Зависимости проекта
├── .env                        # Переменные окружения (создать самостоятельно)
├── Dockerfile                  # Docker образ
├── docker-compose.yml          # Docker Compose конфигурация
├── .dockerignore               # Исключения для Docker
├── handlers/                   # Обработчики сообщений
│   ├── __init__.py
│   └── common.py               # Обработчики команд
├── middlewares/                # Middleware
│   ├── __init__.py
│   └── profanity_middleware.py # Middleware для проверки на мат
└── utils/                      # Утилиты
    ├── __init__.py
    └── statistics.py           # Класс для сбора статистики
```

## Настройка порога

Порог вероятности мата для удаления сообщения настраивается в файле `.env`:

```
PROFANITY_THRESHOLD=0.7
```

- **0.7** (по умолчанию) - средний уровень строгости
- **0.5** - более строгая модерация
- **0.9** - более мягкая модерация

## Статистика

Бот автоматически собирает детализированную статистику своей работы:

### Общая статистика:
- Количество проверенных сообщений
- Количество удаленных сообщений
- Процент удалений
- Детализация по типам удалений:
  - Запрещенные символы
  - За ссылки
  - За мат
- Количество забаненных пользователей

### Статистика по чатам:
- Если бот работает в нескольких чатах, статистика разбивается по каждому чату
- Для каждого чата показывается детализация по типам удалений и количество банов

Статистика отправляется всем админам (указанным в `ADMIN_IDS`) **ежедневно в 21:00** по московскому времени **в личные сообщения**. После отправки статистика автоматически сбрасывается.

### Пример статистики:
```
📊 Статистика работы бота

📅 Период: 01.01.2024 - 01.01.2024 21:00

📝 Всего проверено сообщений: 150
🗑️ Всего удалено сообщений: 25
📈 Процент удалений: 16.7%

Детализация удалений:
🚫 Запрещенные символы: 10
🔗 За ссылки: 8
🗑️ За мат: 7
🔨 Забанено пользователей: 7
```

### Как получить свой user_id для ADMIN_IDS

1. Напишите боту [@userinfobot](https://t.me/userinfobot) в личные сообщения
2. Бот пришлет ваш user_id (это число, например: `123456789`)
3. Добавьте его в `ADMIN_IDS` в файле `.env`

## Безопасность

**Модерация** работает **только** в групповых чатах, указанных в `ALLOWED_CHAT_IDS`. Сообщения из других групп полностью игнорируются. Это гарантирует, что бот не будет модерировать чужие группы без вашего разрешения.

**Команды** бота (например, `/start`) работают **только** в личных сообщениях. Бот не отвечает на команды в групповых чатах, чтобы не засорять чат.

**Исключения из модерации**:
- Администраторы и создатели чата **не проверяются** на мат, ссылки и запрещенные символы
- Их сообщения не удаляются и они не могут быть забанены ботом
- Это гарантирует, что администраторы могут свободно общаться в чате

## Уведомления о бане

При бане пользователя за мат все админы (из `ADMIN_IDS`) получают уведомление в личные сообщения с:
- Информацией о пользователе (имя, username, ID)
- Информацией о чате (название, Chat ID)
- Причиной бана (вероятность мата)
- Текстом сообщения, из-за которого забанили

В уведомлении есть инлайн-кнопки:
- **✅ Разбанить** - разбанивает пользователя в указанном чате
- **🗑️ Скрыть** - удаляет уведомление

## Порядок проверок

Бот проверяет сообщения в следующем порядке:

1. **Проверка прав администратора** - если пользователь админ или создатель чата, проверка пропускается
2. **Проверка запрещенных символов** - если найдены спецсимволы или нерусские буквы (эмодзи разрешены) → удаление сообщения
3. **Проверка ссылок** - если найдены URL, t.me, telegram.me → удаление сообщения
4. **Проверка на мат** - если вероятность мата ≥ порога → удаление сообщения + бан пользователя + уведомление админам

**Для голосовых сообщений**:
- Сначала происходит расшифровка через Google Speech Recognition
- Затем применяются те же проверки, что и для текста

**Примечание**: Эмодзи разрешены в сообщениях и не считаются запрещенными символами. Пользователи могут свободно использовать эмодзи в своих сообщениях.

## Примечания

### Общие
- **Модерация** работает **только** в групповых чатах, указанных в `ALLOWED_CHAT_IDS`
- **Команды** бота работают **только** в личных сообщениях (бот не отвечает в группах)
- Бот должен быть администратором группы с правами на удаление сообщений и бан пользователей
- Администраторы и создатели чата **не проверяются** и не могут быть забанены

### Проверки
- Библиотека check-swear использует ML-модель, обученную на 700,000+ комментариев
- Модель может иметь ложные срабатывания, особенно на необычные слова
- Проверка на запрещенные символы разрешает:
  - Русские буквы (а-я, А-Я, ё, Ё)
  - Цифры (0-9)
  - Знаки препинания: . , ! ? : ; - ( ) [ ] { } " ' « » — …
  - Пробелы
  - **Эмодзи** (все Unicode эмодзи: 😊, 🎉, ❤️, флаги и т.д.)
- Ссылки проверяются в тексте, entities и подписях к медиа

### Голосовые сообщения
- Расшифровка голосовых сообщений требует интернет-соединение (Google Speech Recognition)
- Если расшифровка не удалась, голосовое сообщение **не удаляется** (безопасный режим)
- Для работы расшифровки требуется установленный `ffmpeg`

### Управление
- Чтобы добавить новый чат для модерации, добавьте его chat_id в `ALLOWED_CHAT_IDS` через запятую и перезапустите бота
- Статистика отправляется автоматически в 21:00 ежедневно всем админам из `ADMIN_IDS` в личные сообщения
- После отправки статистики она автоматически сбрасывается и начинается новый день
- Разбан пользователей доступен через инлайн-кнопку в уведомлении о бане

## Лицензия

MIT

